// Shared color values used by both the plain HTML tables and the MUI theme,
// so every table on every page looks the same. Values come from XDS, Meta's
// design system.

// Faint line between table rows and under column headers.
export const LINE_COLOR = 'rgba(5, 54, 89, 0.1)';
// Slightly darker line above a table's "Total" row.
export const TOTAL_LINE_COLOR = 'rgba(5, 54, 89, 0.2)';
// Background shown when the mouse hovers over a row.
export const ROW_HOVER_COLOR = 'rgba(5, 54, 89, 0.05)';
// Dimmed text color for column headers and captions.
export const MUTED_TEXT_COLOR = '#4E606F';
// Colors for a change in value: green when it improved, red when it got worse.
export const IMPROVING_COLOR = '#0d8626';
export const WORSENING_COLOR = '#e3193b';
